'use strict'

const MAP_Url = require('./controllers')
const contribURL
module.exports = (server){
  server.use(['/contributions/:userName'], (req, res, next) => {
    const userName = req.params.userName;
    if(!)
  }
  )
}